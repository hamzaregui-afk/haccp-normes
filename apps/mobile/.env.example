# ─── NORMES HACCP — Mobile App Environment ────────────────────────────────────
# Copy this file to .env and fill in the values.
# Expo reads EXPO_PUBLIC_* variables at build time via process.env.

# API Gateway URL — all requests go through this single entry point
# Dev (Android emulator): http://10.0.2.2:80
# Dev (iOS simulator):    http://localhost:80
# Production (AWS):       https://api.normeshaccp.com
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:80
